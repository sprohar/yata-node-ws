export enum ResourceLimit {
  /**
   * The maximum number of projects that a user can have.
   */
  PROJECT = 10,
  /**
   * The maximum number of sections that a project can have.
   */
  SECTION = 30,
  /**
   * The maximum number of tasks that a project can have.
   */
  TASK = 100,
}
